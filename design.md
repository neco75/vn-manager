# VN Manager — Design specification

更新日: 2026-09-20 / 初版  
対象: [改善ロードマップ #17](https://github.com/neco75/vn-manager/issues/17)  
この文書はこれから実装するUIの基準。現在の実装済み機能を説明するものではない。

## 1. 目的と適用範囲

**作品を探し、自分の体験を記録し、安心して戻ってこられる個人のゲーム書庫。**

主な行動は「登録作品を探す」「作品を追加する」「進捗と感想を更新する」。
ジャケットを眺める楽しさを残しながら、入力・検索・保存を装飾より優先する。
本棚・ルーレット・ランキング・共有は補助機能とする。

- 各Issueは機能仕様・データ互換性・完了条件、本書は共通の視覚・配置・操作規則を定義する。
- 本書を理由に担当Issue外の機能を追加しない。後続Issueの項目は実装が揃うまで表示しない。空の飾りボタンは置かない。
- 明示的な最新のユーザー指示を優先する。本書とIssueに実質的な矛盾があれば、黙って片方を無視せずレビュー時に解消する。
- 既存のNext.js / Tailwind / shadcn・Radix / Lucide / idbを使う。新しいUIライブラリや汎用デザインシステムは作らない。
- UI実装は専用ブランチでPRを作成し、別エージェントのレビューを待つ。実装担当は自己マージ・自動マージ設定をしない。

## 2. 視覚の方向性：静かな書庫

チャコールの背景、少し暖かい白、真鍮色の主要操作。色彩の主役はゲームのジャケット。
棚を模した木目や革のテクスチャを全画面には広げず、通常画面は平面と細い境界で整理する。

**採用する**
- 不透明な画面・カード・入力背景。余白と見出しによる情報のまとまり。
- 読める日本語タイトル、穏やかなアクセント、進捗と自分の評価。
- 同じ動作には同じラベル・色・位置。主要ボタンは1つの操作領域につき原則1個。

**置き換える**
- 既定の紫／ピンクの背景グラデーション、グラデーション文字、発光するボタン。
- 大きなガラス効果、カードの持ち上がりやジャケットのホバー拡大。
- 画像の上にしか出ない説明、hover時しか現れない重要操作。
- 意味なく乱立する色付きアイコン、全ボタンのピル形状、必要以上の巨大見出し。

ダークテーマ1種類を今回の基準とする。ライトテーマはこの改修では追加しない。
ユーザーが設定した背景は維持し、低い不透明度で装飾層に表示する。
本文・フォームの面は不透明とし、画像ぼかし設定を背景にも適用する。既定背景画像は不要。

## 3. デザイントークン

色は `app/globals.css` の既存semantic変数へ集約し、ページごとに似た色を直書きしない。
下記HEX値をCSS変数にそのまま使ってよい。Tailwind v4の既存マッピングを再利用する。

| 既存変数／用途 | 値 | 使用箇所 |
| --- | --- | --- |
| --background | #141619 | ページ背景 |
| --foreground | #ECE8DF | 本文・主要見出し |
| --card / --popover | #1C1F22 | カード・ダイアログ |
| --card-foreground / --popover-foreground | #ECE8DF | 各面の文字 |
| --secondary / --muted / --accent | #25292D | 補助面・hover背景 |
| --secondary-foreground / --accent-foreground | #ECE8DF | 補助面の文字 |
| --muted-foreground | #B3B5B1 | 補足・日付・ブランド |
| --primary / --ring | #D7B77B | 主要ボタン・フォーカス・リンク |
| --primary-foreground | #191B1E | 真鍮色ボタンの文字 |
| --border | #3A3F43 | 装飾的な区切り |
| --input | #7D858C | 入力や未選択コントロールの必要な境界 |
| --destructive | #EFA0A0 | エラー・削除 |
| 危険操作の塗りボタンの文字 | #231619 | 塗りボタンが必要な場合のみ |
| 成功 | #8BC4A0 | 保存完了・完了状態の補助 |
| 情報 | #98BDDA | プレイ中・通信状態の補助 |

`accent`はshadcnのhover面であり、強調色を増やすための変数ではない。
色だけで状態を表現せず、常に文字または名前付きアイコンを伴わせる。
弱い`border`だけをフォームの識別や選択状態の唯一の手掛かりにしない。
destructiveの既存`text-white`は上記の明るい赤に合わないため、UI適用時にforegroundも合わせる。

不透明色でのコントラスト計算例: 本文/カード13.54:1、補足/補助面7.09:1、
主要ボタン文字/塗り9.00:1、入力境界/カード4.42:1。
これらは実画面全体の適合を保証しない。透明度、hover、選択、focusの各状態も実装時に検証する。
通常文字4.5:1以上、意味を持つUI境界・フォーカス3:1以上をレビュー基準とする。

| 項目 | 基準 |
| --- | --- |
| フォント | 現行Geistと日本語システムフォールバック（Yu Gothic / Hiragino Kaku Gothic ProN / Meiryo / sans-serif）。新しいWebフォントは追加しない |
| 本文 | 16px / 行高1.65、400 |
| 画面タイトル | PC 28px / 1.3、スマホ24px / 1.35、600〜700 |
| セクション見出し | 20px / 1.4、600 |
| カードタイトル | PC 16px、スマホ14px / 1.5、600、最大2行 |
| 補足 | 13px / 1.5。操作ラベルは14px以上、入力文字は16px以上 |
| 数値 | 時間・件数・点数はtabular-nums。数字を飾りとして巨大化しない |
| 余白 | 4 / 8 / 12 / 16 / 24 / 32 / 48pxから選ぶ |
| 角丸 | 入力・ボタン8px、カード12px、ダイアログ16px。状態チップのみピル可 |
| --radius | 10px。既存radius-sm/md/lg/xlを上記へ明示的に対応させる |
| 操作領域 | ボタン・アイコン・切替は最低44×44px。表示アイコンは18〜20px |
| 枠線 | 1px。選択状態は文字＋背景または輪郭でも示す |
| 影 | ダイアログ等の浮いた面だけ。カードやCTAに発光させない |
| 動き | 色・opacityを120〜180ms。位置・サイズの装飾アニメーションは不要 |

`prefers-reduced-motion`では不要なアニメーションを止める。
ルーレットの演出も同設定では即座に結果を出し、回転演出を必須にしない。

## 4. 共通の画面枠とナビゲーション

| 幅 | レイアウト |
| --- | --- |
| 320〜639px | 左右16px、1カラム。カード一覧のみ2列、列間12px |
| 640〜767px | 左右24px、カード3列 |
| 768〜1023px | 左右24px、カード4列。ヘッダーはまだメニュー形式 |
| 1024px以上 | 左右32px、本文最大1200pxで中央寄せ、カード5列、列間20px |

詳細は最大1040px、設定は最大760px。縦のセクション間隔はPC32px／スマホ24px。
ヘッダーは高さ64px（スマホ56px）、不透明背景、sticky。横スクロールを発生させない。
固定要素のz-indexはヘッダー30、保存バー20、モーダル50を目安とし、重ね順を無秩序に増やさない。

- PC: ロゴ → ライブラリ / 作品を探す / ランキング / 統計 / 設定。Aboutは設定内へ。
- 1024px未満: ロゴ＋「メニュー」ボタン。既存Dialogでラベル付きのナビ・言語・画像ぼかしをまとめる。
- 画像ぼかしはPCでも設定またはメニュー内の名前付きスイッチとする。ヘッダーに意味不明な赤いスイッチを単独で置かない。
- 現在ページは`aria-current="page"`と背景または下線で示す。hoverだけを現在位置の表示にしない。
- 設定ページが未実装の間はリンクを先に追加しない。既存Aboutを残し、#16で移す。
- 下部ナビと常設サイドバーは今回追加しない。

## 5. ライブラリ

順序は **見出しと追加 → ライブラリ内検索 → フィルター → 件数・並べ替え・表示形式 → 一覧**。
検索対象は登録済み作品。「作品を探す」はVNDB検索であり、検索欄を混同させない。

### PCのワイヤーフレーム

```text
┌ VN Manager ─ ライブラリ / 作品を探す / ランキング / 統計 / 設定 ┐
│ ライブラリ                                      [＋作品を追加] │
│ [ 登録作品のタイトル・別名・ブランドを検索                  ] │
│ [すべて] [プレイ中] [プレイ予定] [クリア済み] [その他 ▾]        │
│ 所有状況 [すべて ▾]       24作品   [追加が新しい順 ▾] [表示 ▾] │
│ ┌表紙──┐ ┌表紙──┐ ┌表紙──┐ ┌表紙──┐ ┌表紙──┐               │
│ │      │ │      │ │      │ │      │ │      │               │
│ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘               │
│ 作品名2行 / ブランド / 状態 / 自分の評価                    │
│ …                                                           │
└─────────────────────────────────────────────────────────────┘
```

### スマホのワイヤーフレーム

```text
┌ VN Manager                 [メニュー] ┐
│ ライブラリ                 [＋追加]   │
│ [登録作品を検索                     ] │
│ 状態 [すべて ▾]   所有 [すべて ▾]      │
│ 24作品 [追加が新しい順 ▾] [表示 ▾]     │
│ ┌表紙────────┐ ┌表紙────────┐        │
│ │            │ │            │        │
│ └────────────┘ └────────────┘        │
│ 作品タイトル   作品タイトル          │
│ 最大2行        最大2行               │
│ ブランド       ブランド              │
│ プレイ中       クリア済み            │
│ 自分 85/100    未評価                │
└──────────────────────────────────────┘
```

図は情報順の基準。文字数に合わせて操作行を折り返し、無理に同じ行へ押し込まない。
PCの「その他」には一時中断・中断・動画視聴を含める。スマホは状態Selectで全値を選べる。
検索中の件数は「一致24件 / 全120件」。条件ありの0件には「条件を解除」を出す。
ルーレットは「次に遊ぶ作品を選ぶ」という補助操作として一覧ツール付近に置き、追加より強調しない。

### カード・リスト・本棚
- カードの画像枠は2:3。ゲームのパッケージ全体を見せるため`object-fit: contain`、余白はcard色。引き伸ばさない。
- タイトルと表紙は詳細へ移動する主リンク。検索結果の追加ボタンはリンク外に置く。
- 本文領域は12〜16px padding、タイトル2行分の高さを確保する。
- 状態は文字で示す。自分の評価は「自分 85/100」または「未評価」。0点を隠さない。
- ライブラリではVNDB評価と発売日は詳細に任せ、カードを情報で埋めない。
- 検索カードではブランドと発売年、任意の「VNDB 8.7/10」を補助表示してよい。自分の点数と混同させない。
- 状態や追加操作を画像のhoverオーバーレイへ置かない。
- 画像なしは同じ比率の静かなプレースホルダーと作品名。画像非表示時も作品を識別できる。
- リストは小表紙48×72px、タイトル、状態、評価。狭い画面では状態・評価をタイトルの下へ折り返す。
- 本棚は既存の鑑賞用表示を維持できるが、ぼかし・キーボード操作・作品名の識別を犠牲にしない。

## 6. 作品検索と追加

- 見出し「作品を探す」と説明「VNDBから検索してライブラリへ追加」。巨大なヒーローは不要。
- 検索欄に可視ラベルと「検索」ボタン。Enterでも送信する。
- 初期状態には短い操作案内、0件には検索語と「別のタイトル・原題で試す」、通信失敗には理由と再試行。
- 結果カードを押すと詳細。独立した「追加」を押すと既定のプレイ予定として保存する。
- 登録済みは「登録済み」と詳細導線。二重追加で記録を上書きしない。
- 状態などを指定する追加フォームは任意。「記録の詳細」で評価・時間・購入先を開ける。
- 一覧の末尾に「もっと見る」。取得中は同じ場所で処理を示し、失敗時も取得済み結果を消さない。
- 追加成功は短い通知＋カード状態の更新。保存失敗は同じ操作の近くにも表示する。

## 7. 作品詳細と記録

DOM順も **戻る → 作品の識別 → 自分の記録 → 外部の作品情報** にする。
「戻る」はライブラリまたは検索の条件付きURLへ戻し、直接アクセス時はライブラリへ。
履歴の位置を推測して無条件に外部ページへ戻さない。

### PCのワイヤーフレーム

```text
[← ライブラリへ]
┌表紙 144×216┐  日本語タイトル（全文）
│            │  原題（異なる場合のみ） / ブランド
└────────────┘  所有済み・プレイ中    VNDBを開く ↗
┌ 自分の記録 ──────────────────┬ 記録の詳細 ───────────────┐
│ 再開メモ [前回どこまで進めたか] │ 状態 [プレイ中 ▾]         │
│ メモ     [書く] [プレビュー]   │ 所有 [所有済み ▾]         │
│ [                             ]│ 自分の評価 [85] /100      │
│ [                             ]│ プレイ時間 [12.5] 時間    │
│ 感想     [書く] [プレビュー]   │ 日付・購入先 [開く ▾]     │
│ [                             ]│                           │
└───────────────────────────────┴───────────────────────────┘
[下書き保存済み・本記録は未保存]                [変更を保存]
[作品情報 ▾] あらすじ・発売日・VNDB評価・安全なタグ・画像・外部リンク
ライブラリから削除（控えめな危険操作）
```

### スマホのワイヤーフレーム

```text
[← ライブラリへ]
┌表紙80×120┐ タイトル全文
│          │ ブランド
└──────────┘ 所有済み / プレイ中
自分の記録
状態 [プレイ中 ▾]   所有 [所有済み ▾]
再開メモ [前回の続き                  ]
メモ     [書く] [プレビュー]
[                                    ]
感想     [書く] [プレビュー]
[                                    ]
[記録の詳細 ▾] 評価・時間・日付・購入先
[作品情報 ▾]
ライブラリから削除
┌ 下書き保存済み     [変更を保存] ┐  ←必要時だけsticky
```

PCの記録領域は`minmax(0, 1fr) 280px`、間隔24px。1024px未満は1列。
既存作品の「変更を保存」は未変更ならdisabled。未登録作品は既定値のまま「ライブラリに追加」可能。
保存中は二重実行を防ぐ。失敗しても入力を消さず、成功後だけ保存状態を更新する。
スマホの保存バーは未保存／保存中／失敗時に出す。safe-areaと下余白を確保し、本文やフォーカスを隠さない。
ソフトキーボード時はスクロールで操作へ到達できることを実機相当で確認する。

- 「下書き保存済み」は本保存と別。表示例「下書き保存済み・記録には未反映」。
- 再訪時の下書きは「復元」「下書きを破棄」を提示。本記録を無断で置き換えない。
- 削除は保存の隣に目立つ赤いアイコンで置かない。作品名と失われる記録を確認し、本体ゲームファイルを削除する操作ではないことを明確にする。
- あらすじは既定で折り畳む。ネタバレ区間・タグは別途明示的に開く。個人記録より先に外部情報を長く並べない。
- 画像を背景に設定する操作は作品情報内の補助操作に移す。

## 8. 統計・ランキング・設定

**統計**
- 上部は登録作品数／クリア本数／自分の平均評価／記録した時間の4項目。スマホは2列。
- 実績時間と推定時間は別ラベル・別集計。推定を実績に混ぜない。
- 続けて月別クリア数、状態別件数、非ネタバレの頻出タグ。頻度比較は横棒を優先する。
- 平均評価は評価済み件数を併記。月別の対象外は「クリア日未設定 N件」。
- 色数を抑え、数値とラベルで読めるようにする。空データは説明と記録への導線。
- 共有対象に期間・単位・集計対象を含め、設定・私的メモ・復元操作を含めない。

**ランキング**
- 見出し「自分のランキング」。VNDB全体の順位と誤認させない。
- 自分の点数、作品名、状態を表示。未評価を除き0点は含める。
- 同点は同順位（1, 1, 3方式）。同点内の表示順は追加日時などで安定させる。点数のない架空順位を作らない。
- タグを表示する場合は非ネタバレのみ。画像書き出しの新設は別Issue。

**設定**
- 順序は「データとバックアップ」「表示と言語」「購入先」「このアプリについて」。
- バックアップ／復元を最初に置き、保存先がこのブラウザであることと端末間同期しないことを短く説明する。
- 最終エクスポート日時は参考情報。「安全にバックアップ済み」と断定しない。
- 復元はファイル選択 → 検証結果・追加/競合件数 → 競合の扱い → 実行 → 結果。
- 「既存を保持」を既定にする。無効な内容を検証前に保存しない。
- 購入先の編集・削除はタッチでも見つかる配置。設定項目を別々の巨大カードにしない。

## 9. 共通インタラクション・文章

| 状態 | 表示と振る舞い |
| --- | --- |
| 初期読み込み | 一覧は最小限のskeleton、フォームは読み込み表示。0件と断定しない |
| 空ライブラリ | 「まだ作品がありません」＋「作品を追加」＋「バックアップから復元」 |
| 絞り込み0件 | 条件と0件を示し「条件を解除」。初期案内へ戻さない |
| 読み込み失敗 | 「ライブラリを読み込めませんでした」＋再試行。正常な空データ扱いしない |
| 保存成功 | 短い通知と保存状態の更新。モーダルは成功後だけ閉じる |
| 保存失敗 | 入力を維持。近くに説明と再試行。トーストだけに頼らない |
| 外部情報取得失敗 | 保存済み情報を維持し「最新情報を取得できませんでした」 |
| 画像制限 | ぼかし＋「画像を非表示」。作品年齢区分を画像の判定に流用しない |
| ネタバレ | 初期は非表示。「ネタバレを表示」で対象だけを開く |

- 主要ラベル: 「作品を追加」「変更を保存」「ライブラリから削除」「画像をぼかす」「書く」「プレビュー」「購入先」。
- watchedの表示は「動画視聴」、on_holdは「一時中断」、droppedは「中断」。データ値は既存のまま。
- 所有状況: unknown「未設定」、owned「所有済み」、wishlist「気になる」。
- 自分の評価は100点満点。外部は「VNDB 8.7/10」。未評価は「未評価」、不明な数値は「—」。
- プレイ時間は「12.5時間」。日付は日本語UIではYYYY/MM/DD、保存はIssueのISO形式に従う。
- 日付だけの値をUTC日時へ変換して前日へずらさない。VNDBの部分日付は「2026年」「2026年9月」等で精度を捏造しない。
- 日本語タイトル優先。別名の優先順位は#11。英語UIも翻訳辞書を通し、html langを連動させる。
- 入力は可視Label＋controlのidで関連付ける。placeholderをLabel代わりにしない。
- 検索欄以外でEnterを押しただけの意図しない保存・削除を避ける。

## 10. アクセシビリティと共通部品

- リンクは移動、buttonは操作。クリック可能なdivで代用しない。リンク内にbuttonを入れない。
- Tab順はDOMの情報順と一致。focus-visibleはprimary色の2px以上の輪郭＋2px offset相当。
- トグルに名前と状態、アコーディオンにaria-expanded/controls、保存通知に適切なlive region。
- スクリーンショットの拡大も既存Dialogを使い、Esc・フォーカス管理・起点への復帰を揃える。
- ダイアログは最大高さcalc(100dvh - 32px)、内部スクロール可能。スマホは左右16pxの余白を確保。
- 非表示のネタバレ本文をopacity:0だけで隠さない。開くまではアクセシビリティツリーにも露出させない。
- 画像のぼかしは表示上の配慮であり、元ファイルへのアクセスを制限する認証機能とは説明しない。
- 200%ズーム、キーボードのみ、reduced-motionで主要フローを確認する。

実装の置き場所:
- 色・フォント・共通面: `app/globals.css`
- 共通ナビ・外枠: `components/ClientLayout.tsx`
- 操作: 既存`components/ui/*`のvariantを調整。ページごとに別ボタンを新設しない。
- カード: `components/VNCard.tsx`。画像の表示ポリシーとタイトル選択は全表示箇所で共通化。
- 日本語表記: `lib/translations.ts`
- ページ配置は各pageに残してよい。1箇所しか使わない巨大な汎用レイアウト部品は不要。

## 11. 段階的な適用とレビュー

| Issue | 本書の主な担当範囲 |
| --- | --- |
| #3〜#9 | 機能・保存・安全性を優先。触った操作のLabelや失敗表示は揃えるが全面着せ替えしない |
| #10 | 色・書体・ボタン等の共通トークンを適用し、検索画面とカードの基礎を整える |
| #11 | 日本語タイトル、ブランド、2行表示 |
| #12 | ライブラリ配置・絞り込み・カード内の情報優先順位 |
| #13 | 共通ナビ、1024px未満のメニュー、操作領域、モーダル |
| #14 | 詳細の情報順・下書き・保存操作 |
| #15 | 統計とランキングの点数・同点表示 |
| #16 | 設定ページ、ナビ最終構成、表記と説明の統一 |

共通トークン適用時は、既存の白いボタン文字や半透明面との組合せを全ページで確認する。
変更途中の一時的な混在は許容するが、新しい独自色・別ルールは増やさない。
各PRは担当箇所について以下を記録し、別エージェントが差分と実画面をレビューする。

- [ ] 320 / 390 / 768 / 1024 / 1280pxで不要なページ横スクロールがない。
- [ ] 長い日本語タイトル、画像なし、ぼかしON、0点、未評価で壊れない。
- [ ] 初期・読込中・空・失敗・成功が区別でき、必要な操作が残る。
- [ ] キーボード操作とフォーカス、画面幅390×667px、200%ズームを確認した。
- [ ] 本書の担当範囲についてPC／スマホのスクリーンショットまたは同等の検証証跡をPRへ添えた。
- [ ] Issueのデータ互換性と機能要件も満たしている。見た目のために保存・安全性を削っていない。

手動登録、ルート進捗、クラウド同期、AI推薦、共有画像の新機能はこの文書だけでは実装しない。
